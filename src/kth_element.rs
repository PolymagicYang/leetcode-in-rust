use crate::Solution;

impl Solution {
    pub fn kth_smallest(matrix: Vec<Vec<i32>>, k: i32) -> i32 {
		let (mut start_outer, mut end_outer) = (0 as usize, matrix.len());

		let a = true;
		match a {
			true => {
				let r = 1;
			},
			false => {

			}
		}
		2
    }
}

#[test]
fn test() {
	print!("{}", 1 << 3);
	
}